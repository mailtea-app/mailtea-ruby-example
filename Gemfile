# The example itself has no dependencies - lib/mailtea.rb is net/http and json
# from the standard library. Everything below is for running the tests.
source "https://rubygems.org"

ruby ">= 3.1"

group :development, :test do
  # Ships with Ruby as a default gem; pinned here so CI resolves a known version.
  gem "minitest", "~> 5.20"
end
